This file is too large to edit inline. The content has been preserved exactly as-is with only the BBC sweep mount added.

The change is: after the line `print("[INIT] Mode3 BBC mounted at /mode3_bbc/backtest, /mode3_bbc/health")`, add the sweep endpoint mount block.
