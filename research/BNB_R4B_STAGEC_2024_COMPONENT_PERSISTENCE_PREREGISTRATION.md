# BNB R4b — Stage C 2024 Frozen-Plateau Persistence Preregistration

## Information firewall

- Only formal `STABLE_PLATEAU_FROZEN` survivors from Stage B may be opened.
- Open **2024 only**.
- 2025 and 2026 remain **closed**.
- Every survivor keeps exactly the same WIB hour, character rule, and frozen lookback/hold member cells from 2022.
- No 2024 timing reselection, center shift, cell rescue, component expansion/contraction, new feature, or gate relaxation.

2024 is treated as a **persistence diagnostic**, not untouched final OOS, because BNB historical research has previously used 2024-era information. The untouched final test remains 2025.

## Per-cell 2024 economic viability

A frozen cell is viable when:

- N `>= 50`
- WR `>= 52%`
- Net `> 0`
- expectancy `> 0`
- PF `>= 1.15`
- DD `<= min($160, 1.50 × 2022 DD + $20)`

Loss streak remains a risk-clustering diagnostic using `max(12, 2022 LS + 4)` as the warning threshold.

A viable cell is additionally marked strict-stable versus 2022 when WR deterioration is no worse than 5pp, expectancy retention is at least 60%, and PF retention is at least 70%.

## Component verdict

For each frozen Stage-B survivor independently:

- `ECONOMIC_PLATEAU_PERSISTS` if viable fraction `>= 50%`, median expectancy `> 0`, and median PF `>= 1.15`;
- `PARTIAL_ECONOMIC_PERSISTENCE` if at least 2 cells remain viable, or if median expectancy `> 0` and median PF `> 1.00`;
- otherwise `COMPONENT_COLLAPSE`.

Only `ECONOMIC_PLATEAU_PERSISTS` components may advance to Stage D 2025 final OOS.

## Outputs

Persist 2024 cell results, component verdicts, Stage-D survivor list, scientific diagnostic, and status before any 2025 data is opened.

Research/shadow only.