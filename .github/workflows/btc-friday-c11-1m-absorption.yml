name: BTC Friday C11A 1m Absorption

on:
  workflow_dispatch:
  push:
    paths:
      - 'research/btc_friday_c11a_1m_absorption.py'
      - 'BTC_Friday_C11A_1m_Absorption_Preregistration.md'
      - '.github/workflows/btc-friday-c11-1m-absorption.yml'

permissions:
  contents: write

concurrency:
  group: btc-friday-c11a-1m-absorption
  cancel-in-progress: true

jobs:
  c11a:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install numpy pandas requests scikit-learn
      - name: Run frozen C11A
        id: run
        continue-on-error: true
        run: |
          set +e
          PYTHONPATH=research python research/btc_friday_c11a_1m_absorption.py > BTC_Friday_C11A_1m_Absorption_Run.log 2>&1
          code=$?
          echo "$code" > BTC_Friday_C11A_1m_Absorption_EXIT_CODE.txt
          cat BTC_Friday_C11A_1m_Absorption_Run.log
          exit $code
      - name: Persist artifacts
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add BTC_Friday_C11A_1m_Absorption_EXIT_CODE.txt BTC_Friday_C11A_1m_Absorption_Run.log
          for f in BTC_Friday_C11A_1m_Absorption_Result.md BTC_Friday_C11A_1m_Absorption_Result.json BTC_Friday_C11A_1m_Absorption_Rows.csv; do
            if [ -f "$f" ]; then git add "$f"; fi
          done
          if ! git diff --cached --quiet; then
            git stash push --include-untracked -m c11a-artifacts
            git pull --rebase origin main
            git stash pop
            git add BTC_Friday_C11A_1m_Absorption_EXIT_CODE.txt BTC_Friday_C11A_1m_Absorption_Run.log
            for f in BTC_Friday_C11A_1m_Absorption_Result.md BTC_Friday_C11A_1m_Absorption_Result.json BTC_Friday_C11A_1m_Absorption_Rows.csv; do
              if [ -f "$f" ]; then git add "$f"; fi
            done
            git commit -m "research: persist BTC Friday 1m absorption C11A result"
            git push
          fi
      - name: Fail if run failed
        if: steps.run.outcome == 'failure'
        run: exit 1
