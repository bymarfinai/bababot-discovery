name: SOL Economic-First H01 one-hour LONG character

on:
  push:
    branches:
      - sol-economic-first-h01-long-08-09wib-character
    paths:
      - '.sol-economic-first-h01-trigger'

jobs:
  sol-h01:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - name: Run preregistered SOL Economic-First H01
        run: |
          set -euo pipefail
          LOG=SOL_ECONOMIC_FIRST_H01_LONG_08_09WIB_CHARACTER_Run.log
          echo 'SOL Economic-First H01 one-hour LONG character discovery' > "$LOG"
          echo "commit=$GITHUB_SHA" >> "$LOG"
          python -m py_compile research/sol_economic_first_h01_long_08_09wib_character.py 2>&1 | tee -a "$LOG"
          python research/sol_economic_first_h01_long_08_09wib_character.py 2>&1 | tee -a "$LOG"
      - uses: actions/upload-artifact@v4
        with:
          name: sol-economic-first-h01-long-08-09wib-character
          path: SOL_ECONOMIC_FIRST_H01_LONG_08_09WIB_CHARACTER_*
          if-no-files-found: warn
