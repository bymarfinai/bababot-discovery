name: BNB B38-S6 Forward Shadow (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic B38-S6 forward collection disabled; historical reference testing is used instead."
