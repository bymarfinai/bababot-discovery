name: B25A
on:
  push:
    paths:
      - '.github/workflows/b25a.yml'
      - 'research/btc_4h_regime_15m_ema_1to1_b25a.py'
      - 'BTC_4H_REGIME_15M_EMA_1TO1_B25A_Preregistration.md'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Runner heartbeat
        run: |
          echo "RUNNING run_id=${GITHUB_RUN_ID} sha=${GITHUB_SHA} started_utc=$(date -u +%Y-%m-%dT%H:%M:%SZ)" > BTC_4H_REGIME_15M_EMA_1TO1_B25A_Status.txt
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add BTC_4H_REGIME_15M_EMA_1TO1_B25A_Status.txt
          git commit -m 'ci: mark B25A runner started' || true
          git push || true
      - run: pip install numpy pandas requests
      - run: python -m py_compile research/btc_4h_regime_15m_ema_1to1_b25a.py
      - run: cd research && python btc_4h_regime_15m_ema_1to1_b25a.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git pull --rebase
          git add BTC_4H_REGIME_15M_EMA_1TO1_B25A_Result.md BTC_4H_REGIME_15M_EMA_1TO1_B25A_Summary.csv BTC_4H_REGIME_15M_EMA_1TO1_B25A_Trades.csv
          git commit -m 'research: persist B25A fixed 1to1 result' || true
          git push
