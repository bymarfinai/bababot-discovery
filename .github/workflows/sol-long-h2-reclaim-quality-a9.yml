name: SOL LONG H2 Reclaim Quality A9

on:
  push:
    branches:
      - research/sol-long-structure-a1-run
    paths:
      - 'SOL_LONG_H2_RECLAIM_QUALITY_A9_Preregistration.md'
      - 'research/sol_long_h2_reclaim_quality_a9.py'
      - '.github/workflows/sol-long-h2-reclaim-quality-a9.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  a9:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout research branch
        uses: actions/checkout@v4
        with:
          ref: research/sol-long-structure-a1-run
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install research dependencies
        run: pip install -r requirements.txt pandas
      - name: Run A9 reclaim quality anatomy
        run: python research/sol_long_h2_reclaim_quality_a9.py
      - name: Persist A9 outputs
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add SOL_LONG_H2_RECLAIM_QUALITY_A9_Result.md \
                  SOL_LONG_H2_RECLAIM_QUALITY_A9_ANATOMY.csv \
                  SOL_LONG_H2_RECLAIM_QUALITY_A9_SNAPSHOTS.csv \
                  SOL_LONG_H2_RECLAIM_QUALITY_A9_SEPARATION.csv \
                  SOL_LONG_H2_RECLAIM_QUALITY_A9_Status.txt
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'research: persist SOL H2 reclaim quality A9 results'
          for i in 1 2 3 4; do
            git pull --rebase origin research/sol-long-structure-a1-run && \
            git push origin HEAD:research/sol-long-structure-a1-run && exit 0
            sleep $((i * 2))
          done
          exit 1
