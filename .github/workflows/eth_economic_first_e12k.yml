name: ETH Economic-First E12K one-hour LONG character

on:
  push:
    branches:
      - eth-e12k-long-23-00wib-character
    paths:
      - '.eth-e12k-trigger'

jobs:
  e12k:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - name: Run preregistered ETH Economic-First E12K
        run: |
          set -euo pipefail
          LOG=ETH_ECONOMIC_FIRST_E12K_LONG_23_00WIB_CHARACTER_Run.log
          echo 'ETH Economic-First E12K one-hour LONG character discovery' > "$LOG"
          echo "commit=$GITHUB_SHA" >> "$LOG"
          python -m py_compile research/eth_economic_first_e12k_long_23_00wib_character.py 2>&1 | tee -a "$LOG"
          python research/eth_economic_first_e12k_long_23_00wib_character.py 2>&1 | tee -a "$LOG"
      - uses: actions/upload-artifact@v4
        with:
          name: eth-economic-first-e12k-long-23-00wib-character
          path: ETH_ECONOMIC_FIRST_E12K_LONG_23_00WIB_CHARACTER_*
          if-no-files-found: warn
