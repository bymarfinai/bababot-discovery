name: SOL Economic-First H14 one-hour LONG character

on:
  push:
    branches:
      - sol-economic-first-h14-long-21-22wib-character
    paths:
      - '.sol-economic-first-h14-trigger'

jobs:
  sol-h14:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - name: Run preregistered SOL Economic-First H14
        run: |
          set -euo pipefail
          LOG=SOL_ECONOMIC_FIRST_H14_LONG_21_22WIB_CHARACTER_Run.log
          echo 'SOL Economic-First H14 one-hour LONG character discovery' > "$LOG"
          echo "commit=$GITHUB_SHA" >> "$LOG"
          python -m py_compile research/sol_economic_first_h14_long_21_22wib_character.py 2>&1 | tee -a "$LOG"
          python research/sol_economic_first_h14_long_21_22wib_character.py 2>&1 | tee -a "$LOG"
      - uses: actions/upload-artifact@v4
        with:
          name: sol-economic-first-h14-long-21-22wib-character
          path: SOL_ECONOMIC_FIRST_H14_LONG_21_22WIB_CHARACTER_*
          if-no-files-found: warn
