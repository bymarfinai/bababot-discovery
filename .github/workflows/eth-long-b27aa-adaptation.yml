name: ETH LONG B27AA Adaptation

on:
  push:
    branches:
      - research/eth-long-b27aa-adaptation
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-b27aa-adapt:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/eth-long-b27aa-adaptation
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install research dependencies
        run: python -m pip install --upgrade pip pandas numpy requests
      - name: Run ETH LONG B27AA reclaim adaptation
        shell: bash
        run: |
          set -o pipefail
          python research/eth_long_f75_early_reclaim_b27aa_adapt.py 2>&1 | tee ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Run.log
      - name: Persist outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Result.md \
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Trades.csv \
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Summary.csv \
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Status.txt \
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Run.log
          git commit -m "persist ETH LONG B27AA adaptation outputs"
          git push origin HEAD:research/eth-long-b27aa-adaptation
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: eth-long-b27aa-adaptation
          path: |
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Result.md
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Trades.csv
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Summary.csv
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Status.txt
            ETH_LONG_F75_EARLY_RECLAIM_B27AA_ADAPT_Run.log
