name: BTC Temporal Friday A6.2 All Clock Discovery

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday-a62:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday A6.2 all-clock discovery validation
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday_a62_allclock_discovery.py
