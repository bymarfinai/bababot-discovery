name: BTC Temporal Friday15 A6.30 Conditional Tight Stop

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a630:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.30 conditional tight stop
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a630_conditional_tight_stop.py
