name: BTC Temporal Friday15 A6.13 Giveback Rescue

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a613:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.13 executable giveback rescue
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a613_giveback_rescue.py
