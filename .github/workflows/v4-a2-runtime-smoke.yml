name: BTC Temporal Friday15 A6.36 Max Drawdown Forensics

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a636:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.36 max drawdown forensics
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a636_maxdd_forensics.py
