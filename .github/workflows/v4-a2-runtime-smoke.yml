name: BTC Temporal A5 Champion Loss Forensics

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a5:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A5 champion loss forensics
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a5_champion_forensics.py
