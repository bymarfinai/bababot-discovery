name: BTC Temporal Friday17 A6.35 Independent Discovery

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday17-a635:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday17 A6.35 independent discovery
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday17_a635_independent_discovery.py
