name: BTC Temporal Friday15 A6.5 Prepump Transfer

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a65:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.5 frozen pre-pump transfer
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a65_prepump_transfer.py
