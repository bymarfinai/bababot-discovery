name: BTC Temporal A4.3b Static Frontier

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a43b:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A4.3b static WR PnL frontier
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a43b_static_frontier.py
