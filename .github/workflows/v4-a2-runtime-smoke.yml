name: BTC Temporal A5.3 Frozen Robustness

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a53:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A5.3 frozen robustness
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a53_frozen_robustness.py
