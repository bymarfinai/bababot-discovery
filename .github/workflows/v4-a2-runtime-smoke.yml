name: BTC Temporal Friday15 A6.4 Short Horizon

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a64:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.4 short-horizon geometry
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a64_short_horizon.py
