name: BTC Temporal Friday A6.1 Cluster Stability

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday-a61:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday A6.1 cluster stability
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday_a61_cluster_stability.py
