name: BTC Temporal Friday15 A6.16 Distribution Robustness

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a616:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.16 distribution robustness
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a616_distribution_robustness.py
