name: BTC Temporal Friday15 A6.49 Regime Direction Switch

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-a649:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday15 A6.49 regime direction switch
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_a649_regime_direction_switch.py
