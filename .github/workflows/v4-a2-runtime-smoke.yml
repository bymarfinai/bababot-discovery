name: BTC Temporal A5.7 Giveback Sequence

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a57:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A5.7 giveback sequence forensics
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a57_giveback_sequence.py
