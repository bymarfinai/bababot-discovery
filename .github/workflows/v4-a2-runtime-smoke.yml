name: BTC Temporal A3.3 Microflow Direction Run

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a33:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Run A3.3 taker-flow dynamic direction
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a33_microflow.py
