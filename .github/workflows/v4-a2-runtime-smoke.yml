name: BTC Temporal A5.10 Runner Recovery

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a510:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A5.10 runner recovery
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a510_runner_recovery.py
