name: BTC Temporal Friday15 T-Method F5.5 Fixed Conjunction Robustness

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday15-f55:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday T-Method F5.5 fixed conjunction robustness
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday15_f55_fixed_conjunction_robustness.py
