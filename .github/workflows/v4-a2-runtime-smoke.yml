name: BTC Temporal A5.9 Frozen FastMR Robustness

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a59:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A5.9 frozen fastMR robustness
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a59_frozen_fastmr_robustness.py
