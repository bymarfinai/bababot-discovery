name: BTC Temporal A5.8 Fast Mean Reversion

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a58:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run A5.8 fast mean-reversion rescue
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a58_fast_mean_reversion.py
