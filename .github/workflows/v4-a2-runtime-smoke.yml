name: BTC Temporal Friday A6.3 Walk Forward Clock

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday-a63:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday A6.3 walk-forward clock rotation
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday_a63_walkforward_clock.py
