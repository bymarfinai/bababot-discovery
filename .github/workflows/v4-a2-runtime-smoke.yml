name: BTC Temporal Friday16 A6.34 Independent Discovery

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  friday16-a634:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run Friday16 A6.34 independent discovery
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_friday16_a634_independent_discovery.py
