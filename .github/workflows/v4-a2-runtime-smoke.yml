name: BTC Temporal A3.9 Tuesday Money Local Refine

on:
  push:
    paths:
      - '.github/workflows/v4-a2-runtime-smoke.yml'

jobs:
  temporal-a39:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Run A3.9 local money refinement
        shell: bash
        run: |
          set -euo pipefail
          python btc_temporal_a39_local_refine.py
