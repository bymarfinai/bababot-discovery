name: SOL Adaptive Risk Manager V1

on:
  push:
    branches: [sol-adaptive-risk-manager-v1]
    paths:
      - 'research/sol_adaptive_risk_manager_v1.py'
      - 'research/SOL_ADAPTIVE_RISK_MANAGER_V1_PREREG.md'
      - '.github/workflows/sol_adaptive_risk_manager_v1.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  risk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sol-adaptive-risk-manager-v1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install pandas numpy requests scipy scikit-learn
      - name: Compile
        run: python -m py_compile research/sol_adaptive_risk_manager_v1.py
      - name: Run adaptive risk manager discovery
        run: python research/sol_adaptive_risk_manager_v1.py
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sol-adaptive-risk-manager-v1
          if-no-files-found: error
          path: |
            SOL_ADAPTIVE_RISK_MANAGER_V1_*.csv
            SOL_ADAPTIVE_RISK_MANAGER_V1_*.md
            SOL_ADAPTIVE_RISK_MANAGER_V1_*.txt
            research/SOL_ADAPTIVE_RISK_MANAGER_V1_PREREG.md
      - name: Persist result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add SOL_ADAPTIVE_RISK_MANAGER_V1_*.csv SOL_ADAPTIVE_RISK_MANAGER_V1_*.md SOL_ADAPTIVE_RISK_MANAGER_V1_*.txt
          if git diff --cached --quiet; then
            echo "No generated changes to persist"
          else
            git commit -m "persist SOL adaptive risk manager v1 result"
            git pull --rebase origin sol-adaptive-risk-manager-v1
            git push origin HEAD:sol-adaptive-risk-manager-v1
          fi
