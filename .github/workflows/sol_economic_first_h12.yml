name: SOL Economic-First H12 one-hour LONG character

on:
  push:
    branches:
      - sol-economic-first-h12-long-19-20wib-character
    paths:
      - '.sol-economic-first-h12-trigger'

jobs:
  sol-h12:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - name: Run preregistered SOL Economic-First H12
        run: |
          set -euo pipefail
          LOG=SOL_ECONOMIC_FIRST_H12_LONG_19_20WIB_CHARACTER_Run.log
          echo 'SOL Economic-First H12 one-hour LONG character discovery' > "$LOG"
          echo "commit=$GITHUB_SHA" >> "$LOG"
          python -m py_compile research/sol_economic_first_h12_long_19_20wib_character.py 2>&1 | tee -a "$LOG"
          python research/sol_economic_first_h12_long_19_20wib_character.py 2>&1 | tee -a "$LOG"
      - uses: actions/upload-artifact@v4
        with:
          name: sol-economic-first-h12-long-19-20wib-character
          path: SOL_ECONOMIC_FIRST_H12_LONG_19_20WIB_CHARACTER_*
          if-no-files-found: warn
