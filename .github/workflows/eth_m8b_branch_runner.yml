name: ETH M8B economic stop-target crossgrid

on:
  push:
    branches:
      - eth-transfer-m8b-economic-crossgrid
  workflow_dispatch:

permissions:
  contents: read

jobs:
  m8b:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - name: Run ETH M8B crossgrid v2
        shell: bash
        run: |
          test -f research/eth_m8b_crossgrid_run.py
          python -m py_compile research/eth_m8b_crossgrid_run.py
          python research/eth_m8b_crossgrid_run.py
      - uses: actions/upload-artifact@v4
        with:
          name: eth-m8b-economic-crossgrid-v2
          path: |
            ETH_F85_F15_TRANSFER_M2_PRE_H2_ENTRY_GRID_M8B_*
