name: F6.35 GitHub Actions Smoke Test
on:
  push:
    paths:
      - '.github/workflows/f630-analysis.yml'
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke
        run: echo "GitHub Actions runner reached successfully"
